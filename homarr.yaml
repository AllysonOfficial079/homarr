#---------------------------------------------------------------------#
#     Homarr - A simple, yet powerful dashboard for your server.      #
#  You can use this in your "Stacks" of Portainer, create a custom    #
#  stack and copy this YAML code and deploy into your own Docker      # 
#---------------------------------------------------------------------#
services:
  homarr:
    container_name: homarr
    image: ghcr.io/homarr-labs/homarr:latest
    restart: unless-stopped
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock # Optional, only if you want docker integration
      - /portainer/Files/AppData/Config/homarr/appdata:/appdata
    environment:
      - SECRET_ENCRYPTION_KEY="GENERATE YOUR ENCRYPTION KEY"
      # use the command openssl rand -hex 32
    ports:
      - '7575:7575'
